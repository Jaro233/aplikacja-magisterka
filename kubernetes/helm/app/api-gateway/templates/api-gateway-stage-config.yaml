# ConfigMap for staging
apiVersion: v1
kind: ConfigMap
metadata:
  name: api-gateway-green-config
  namespace: {{ .Values.microservice.namespace }}
data:
  SPRING_PROFILES_ACTIVE: {{ if eq .Values.productionSlot "blue" }}kubernetes-stage{{ else }}kubernetes{{ end }}